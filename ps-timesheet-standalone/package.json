{
  "name": "ps-timesheet-standalone",
  "version": "1.0.0",
  "description": "Standalone recreation of the PS TimeSheet Jira worklog pivot logic — fetches Jira worklogs and builds a per-person, per-project, per-month pivot table, independent of any internal platform.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
